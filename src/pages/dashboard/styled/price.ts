import styled from 'styled-components'

export const StyledPrice = styled.span`
    color: white;
    font-weight: bold;
    font-size: 26px;
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
`
